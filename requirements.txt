# UZI 投研终端 · 运行时依赖（Docker / 生产部署用）
# 注意：本地开发请以 pyproject.toml 为唯一事实来源（`pip install -e ".[dev]"`），
# 本文件与 pyproject.toml 的 [project.dependencies] 保持同步，供 Docker 精简安装。

# ── 运行时 ──
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic-settings>=2.0

# ── 可选：实时行情（不安装则自动回退到 demo 数据源）──
# akshare>=1.12

# ── 测试 ──
pytest>=8.0
httpx>=0.27
